'use client';

import TokenCalculator from '@/screens/TokenCalculator/TokenCalculator';

export default function TokenCalculatorLayout({ children }) {
  return <TokenCalculator>{children}</TokenCalculator>;
}
